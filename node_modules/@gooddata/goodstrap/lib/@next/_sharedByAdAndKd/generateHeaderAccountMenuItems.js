"use strict";

var _Object$defineProperty = require("@babel/runtime-corejs2/core-js/object/define-property");

_Object$defineProperty(exports, "__esModule", {
  value: true
});

exports.generateHeaderAccountMenuItems = generateHeaderAccountMenuItems;

function generateHeaderAccountMenuItems(workspacePermissions, // bootstrapResource.current.projectPermissions
uiSettings, // bootstrapResource.settings
workspaceId) {
  var canInitData = workspacePermissions.canInitData;
  var displayAccountPage = uiSettings.displayAccountPage;
  var accountMenuItems = [];
  var accountItem = {
    key: "gs.header.account",
    className: "s-account",
    href: "/#s=/gdc/projects/".concat(workspaceId, "|accountPage|")
  };
  var dataIntegrationConsoleItem = {
    key: "gs.header.dic",
    className: "s-dic",
    href: "/admin/disc/"
  };
  var logoutItem = {
    key: "gs.header.logout",
    className: "s-logout"
  };
  var showAccountItem = workspaceId && displayAccountPage;
  var showDataIntegrationConsoleItem = canInitData === true;

  if (showAccountItem) {
    accountMenuItems.push(accountItem);
  }

  if (showDataIntegrationConsoleItem) {
    accountMenuItems.push(dataIntegrationConsoleItem);
  }

  accountMenuItems.push(logoutItem);
  return accountMenuItems;
}