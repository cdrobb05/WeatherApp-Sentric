import mapboxgl from "mapbox-gl";
import { IGeoData, IGeoConfig } from "../../../interfaces/GeoChart";
export declare function createPushpinFilter(selectedSegmentItems: string[]): mapboxgl.Expression;
export declare function createPushpinDataLayer(dataSourceName: string, geoData: IGeoData, config: IGeoConfig): mapboxgl.Layer;
/**
 * Create layer for clustered points/pins which have 'properties.point_count' indicates number of same points is clustered together
 * @param dataSourceName
 */
export declare function createClusterPoints(dataSourceName: string): mapboxgl.Layer;
/**
 * Create layer for cluster labels which indicate number of points/pins is clustered
 * @param dataSourceName
 */
export declare function createClusterLabels(dataSourceName: string): mapboxgl.Layer;
/**
 * Create layer for un-clustered points which are not close to others
 * @param dataSourceName
 * @param selectedSegmentItems
 */
export declare function createUnclusterPoints(dataSourceName: string): mapboxgl.Layer;
